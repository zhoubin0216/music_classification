# Recommended uv setup:
#   uv venv --python 3.10
#   source .venv/bin/activate
#   uv pip install -r requirements.txt
#
# These dependencies cover the project feature extraction script and the
# Hugging Face MERT inference/conversion scripts. Full MERT training still
# needs fairseq installed from source as described in src/MERT/README.md.

numpy>=1.24,<2.0
torch
torchaudio
transformers==4.38.0
huggingface-hub>=0.20
datasets>=2.18
tqdm>=4.66
soundfile>=0.12
scikit-learn>=1.3
matplotlib>=3.8
pydantic>=2,<3
encodec>=0.1.1
nnAudio>=0.3.3
